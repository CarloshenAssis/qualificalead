import{NextResponse}from'next/server';import{readEmailConfig}from'@/lib/email/config';import{emailWebhookSchema,verifyEmailWebhook}from'@/lib/email/webhook';
export async function POST(request:Request){const body=await request.text(),config=readEmailConfig();if(!verifyEmailWebhook(body,request.headers.get('x-webhook-signature'),config.EMAIL_WEBHOOK_SECRET))return NextResponse.json({error:'unauthorized'},{status:401});const event=emailWebhookSchema.parse(JSON.parse(body));return NextResponse.json({accepted:true,eventKey:`resend:${event.id}`,providerMessageId:event.data.email_id},{status:202});}
