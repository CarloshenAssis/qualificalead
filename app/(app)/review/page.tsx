import { decideLead } from './actions';import { createClient,requireUser } from '@/lib/supabase/server';import { Button,Card,Badge } from '@/components/ui';
type ReviewRow={id:string;companies:{name:string;website:string|null;phone:string|null;email:string|null}|null;qualification_results:{opportunity_score:number;primary_gap:string|null;recommended_offer:string|null;evidence_ids:string[]}|null};
export const dynamic='force-dynamic';
export default async function ReviewPage(){const user=await requireUser();const db=await createClient();const {data}=await db.from('campaign_leads').select('id,stage,companies(name,website,phone,email),qualification_results(opportunity_score,primary_gap,recommended_offer,evidence_ids)').eq('user_id',user.id).eq('stage','REVIEW_PENDING');const leads=(data??[]) as unknown as ReviewRow[];return <div className="flex flex-col gap-5"><header><h1 className="text-xl font-semibold">Fila de revisão humana</h1><p className="text-sm text-ink-mute">Revise fonte, evidências, contato e mensagem antes de aprovar.</p></header>{leads.map((lead)=><Card key={lead.id}><div className="flex justify-between"><div><p className="font-semibold">{lead.companies?.name}</p><p className="text-sm text-ink-mute">{lead.companies?.email??'Sem e-mail'} · {lead.companies?.phone??'Sem telefone'}</p><a className="text-sm text-brand-700 underline" href={lead.companies?.website??'#'} target="_blank" rel="noreferrer">Evidência/site</a></div><div className="text-right"><Badge tone="attention">{lead.qualification_results?.primary_gap??'PESQUISAR'}</Badge><p className="text-sm">Score {lead.qualification_results?.opportunity_score??'—'}</p><p className="text-xs text-ink-mute">{lead.qualification_results?.recommended_offer}</p></div></div><div className="mt-4 rounded-lg bg-canvas p-3 text-sm"><strong>Preview dry-run:</strong> mensagem persistida; nenhum provedor será chamado enquanto o envio real estiver bloqueado.</div><div className="mt-4 flex flex-wrap gap-2">{(['REJECT','RESEARCH_MORE','DO_NOT_CONTACT'] as const).map(decision=><form action={decideLead} key={decision}><input type="hidden" name="id" value={lead.id}/><input type="hidden" name="decision" value={decision}/><Button variant={decision==='DO_NOT_CONTACT'?'danger':'secondary'}>{decision}</Button></form>)}<form action={decideLead}><input type="hidden" name="id" value={lead.id}/><input type="hidden" name="decision" value="APPROVE"/><label className="mr-2 text-sm"><input required type="checkbox" name="confirm" value="yes"/> Confirmo aprovação individual</label><Button variant="positive">Aprovar e-mail</Button></form></div></Card>)}</div>}
